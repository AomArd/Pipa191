name: Build Custom Kernel - Mi Pad 6 (pipa)

on:
  workflow_dispatch:
    inputs:
      SUBLEVEL:
        description: 'Kernel SUBLEVEL (versi spoof)'
        required: true
        default: '191'
        type: choice
        options:
          - '191'
          - '157'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Set up environment
        run: |
          sudo apt-get update -y
          sudo apt-get install -y bc bison build-essential ccache curl flex \
            g++-multilib gcc-multilib git gnupg gperf imagemagick \
            lib32ncurses5-dev lib32readline-dev lib32z1-dev libelf-dev \
            liblz4-tool libncurses5 libncurses5-dev libsdl1.2-dev \
            libssl-dev libxml2 libxml2-utils lzop pngcrush rsync \
            schedtool squashfs-tools xsltproc zip zlib1g-dev \
            python3 cpio kmod

      - name: Clone Kernel Source
        run: |
          git clone --depth=1 \
            https://github.com/crdroidandroid/android_kernel_xiaomi_sm8250 \
            -b 16.0-pipa \
            kernel

      - name: Download Clang Toolchain
        run: |
          mkdir -p clang
          wget -q "https://github.com/ZyCromerZ/Clang/releases/download/17.0.0-20230628-release/Clang-17.0.0-20230628.tar.gz" -O clang.tar.gz
          tar -xzf clang.tar.gz -C clang

      - name: Download GCC Toolchain
        run: |
          git clone --depth=1 \
            https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9 \
            -b lineage-19.1 gcc64
          git clone --depth=1 \
            https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9 \
            -b lineage-19.1 gcc32

      - name: Spoof Kernel SUBLEVEL
        run: |
          cd kernel
          SUBLEVEL_TARGET=${{ github.event.inputs.SUBLEVEL }}
          sed -i "s/^SUBLEVEL = .*/SUBLEVEL = ${SUBLEVEL_TARGET}/" Makefile
          echo "=== Verifikasi ==="
          grep "^VERSION\|^PATCHLEVEL\|^SUBLEVEL" Makefile

      - name: Find defconfig
        run: |
          cd kernel
          echo "=== Mencari defconfig pipa ==="
          find arch/arm64/configs -name "*pipa*" 2>/dev/null || true
          find arch/arm64/configs -name "*kona*" 2>/dev/null || true

          if [ -f "arch/arm64/configs/vendor/pipa_defconfig" ]; then
            echo "DEFCONFIG=vendor/pipa_defconfig" >> $GITHUB_ENV
          elif [ -f "arch/arm64/configs/pipa_defconfig" ]; then
            echo "DEFCONFIG=pipa_defconfig" >> $GITHUB_ENV
          elif [ -f "arch/arm64/configs/vendor/kona_defconfig" ]; then
            echo "DEFCONFIG=vendor/kona_defconfig" >> $GITHUB_ENV
          else
            FOUND=$(find arch/arm64/configs -name "*.defconfig" | head -1)
            echo "DEFCONFIG=$(basename $FOUND .defconfig)" >> $GITHUB_ENV
          fi

      - name: Generate .config
        run: |
          export PATH=$(pwd)/clang/bin:$(pwd)/gcc64/bin:$(pwd)/gcc32/bin:$PATH
          cd kernel
          mkdir -p ../out
          echo "Menggunakan defconfig: ${{ env.DEFCONFIG }}"
          make O=../out \
            ARCH=arm64 \
            SUBARCH=arm64 \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-android- \
            CROSS_COMPILE_ARM32=arm-linux-androideabi- \
            LLVM=1 \
            LLVM_IAS=1 \
            ${{ env.DEFCONFIG }}

      - name: Build Kernel
        run: |
          export PATH=$(pwd)/clang/bin:$(pwd)/gcc64/bin:$(pwd)/gcc32/bin:$PATH
          cd kernel
          make -j$(nproc --all) \
            O=../out \
            ARCH=arm64 \
            SUBARCH=arm64 \
            CC=clang \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-android- \
            CROSS_COMPILE_ARM32=arm-linux-androideabi- \
            LLVM=1 \
            LLVM_IAS=1 \
            2>&1 | tee ../build.log

      - name: Check Build Result
        run: |
          if [ -f "out/arch/arm64/boot/Image.gz-dtb" ]; then
            echo "KERNEL_IMAGE=out/arch/arm64/boot/Image.gz-dtb" >> $GITHUB_ENV
            echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
          elif [ -f "out/arch/arm64/boot/Image.gz" ]; then
            echo "KERNEL_IMAGE=out/arch/arm64/boot/Image.gz" >> $GITHUB_ENV
            echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
          elif [ -f "out/arch/arm64/boot/Image" ]; then
            echo "KERNEL_IMAGE=out/arch/arm64/boot/Image" >> $GITHUB_ENV
            echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
          else
            echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
            echo "Build GAGAL - tidak ada kernel image"
            exit 1
          fi

      - name: Clone AnyKernel3
        if: env.BUILD_SUCCESS == 'true'
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3

      - name: Package Kernel
        if: env.BUILD_SUCCESS == 'true'
        run: |
          cp ${{ env.KERNEL_IMAGE }} AnyKernel3/
          if [ -d "out/arch/arm64/boot/dts" ]; then
            find out/arch/arm64/boot/dts -name "*.dtb" -exec cp {} AnyKernel3/ \; 2>/dev/null || true
          fi
          cd AnyKernel3
          SUBLEVEL=${{ github.event.inputs.SUBLEVEL }}
          DATE=$(date +%Y%m%d)
          ZIP_NAME="CustomKernel-pipa-4.19.${SUBLEVEL}-crDroid16-${DATE}.zip"
          sed -i 's/do.devicecheck=1/do.devicecheck=0/' anykernel.sh
          zip -r9 ../${ZIP_NAME} . -x "*.git*"
          echo "ZIP_NAME=${ZIP_NAME}" >> $GITHUB_ENV

      - name: Upload Kernel ZIP
        if: env.BUILD_SUCCESS == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: CustomKernel-pipa-4.19.${{ github.event.inputs.SUBLEVEL }}
          path: |
            ${{ env.ZIP_NAME }}
            build.log
          retention-days: 7

      - name: Upload build log jika gagal
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-error
          path: build.log
          retention-days: 3

      - name: Summary
        if: always()
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Device:** Mi Pad 6 (pipa)" >> $GITHUB_STEP_SUMMARY
          echo "- **ROM Base:** crDroid 12.5 Android 16" >> $GITHUB_STEP_SUMMARY
          echo "- **Kernel Version Spoofed:** 4.19.${{ github.event.inputs.SUBLEVEL }}" >> $GITHUB_STEP_SUMMARY
          
